## What this changes

<!-- One or two sentences. The diff says what; say why. -->

## Why

<!-- What was wrong, or what was missing. If you tried an obvious approach and it
     did not work, that is worth a line — it saves the next person the same walk. -->

## How it was verified

<!-- Which tests, which browsers, which storage backends. "Ran the suite" is fine
     if that is genuinely all that was needed; say so rather than leaving it blank. -->

- [ ] `pnpm lint` and `pnpm typecheck` are clean
- [ ] `pnpm test` passes
- [ ] A test fails without this change (for a bug fix)
- [ ] Browser tests pass, or this cannot affect them
